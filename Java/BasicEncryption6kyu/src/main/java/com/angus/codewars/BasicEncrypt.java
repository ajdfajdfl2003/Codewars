package com.angus.codewars;

public class BasicEncrypt {
    public String encrypt(String text, int rule) {
        if (text.isEmpty()) {
            return "";
        }
        return null;
    }
}
